# Runtime models

Generated GLB files are written here by the Blender export scripts. The three
expected paths are `characters/hero.glb`, `buildings/turret_basic.glb` and
`buildings/wall_gate.glb`. They are intentionally absent until Blender is
installed and `npm run art:export` is run; the browser then uses procedural
fallbacks.
