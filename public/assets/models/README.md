# Runtime models

Generated GLB files are written here by the Blender export scripts. The three
expected paths include the authored character library, buildings, and
`environment/resource_tree.glb` plus `environment/resource_rock.glb`. They are
generated by `npm run art:export`; if any file is missing or fails the authored
contract, the browser keeps the existing procedural fallback.
