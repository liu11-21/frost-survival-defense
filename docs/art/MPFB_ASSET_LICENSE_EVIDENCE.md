# MPFB / MakeHuman asset licence evidence

Retrieved **2026-08-07**. Official MakeHuman Community sources only — the
project website and the `makehumancommunity` GitHub organization. No
third-party mirrors, no search-engine results.

## Position

| What | Licence |
|---|---|
| **MPFB add-on code** | `GPL-3.0-or-later` — per the extensions.blender.org catalogue entry, the package `blender_manifest.toml`, and `LICENSE.CODE.md` in the official repository |
| **Bundled core graphical assets** (base mesh, targets, rigs, skins, UV layers, textures) | **CC0 1.0 Universal** — per `LICENSE.ASSETS.md` in the official repository |
| **Third-party contributed assets** | **None used.** Nothing outside the assets bundled in the official MPFB extension has been installed or referenced. |

### The separation that matters

The add-on's GPL applies to MPFB's **code**, which runs inside Blender at
authoring time. It is a build-time tool, exactly like Blender itself.

**No MPFB code is packaged into the game runtime.** The game ships only the
exported model — geometry, weights and textures derived from the CC0 core
assets. `scripts/blender/` is authoring tooling and is not bundled by Vite
into `dist/`; the runtime loads `.glb` files and nothing else.

This is the same relationship the project already has with Blender: a
GPL/GPL-compatible tool produces an asset, and the asset is not a derivative
work of the tool's source.

## Retrieved evidence

Stored in `third-party-notices/mpfb/`. Byte counts and SHA-256 are of the
exact responses received.

| File | Canonical source | Bytes | SHA-256 |
|---|---|---|---|
| `LICENSE.ASSETS.md` | `https://raw.githubusercontent.com/makehumancommunity/mpfb2/master/LICENSE.ASSETS.md` | 6,962 | `f6089cba01cb570a24712b41ab8a586c…` |
| `LICENSE.CODE.md` | `https://raw.githubusercontent.com/makehumancommunity/mpfb2/master/LICENSE.CODE.md` | 34,940 | `26c03bc049c15fc5b9e52d791a06f1a6…` |
| `makehuman-license.html` | `http://www.makehumancommunity.org/content/license.html` | 57,957 | `615e83c65b7b0b88abb4d031a72c5d66…` |

Full hashes and the HTTP final locations are in
`.runtime/mpfb/licence-sources.json`, regenerated by the fetch step.

`LICENSE.ASSETS.md` is the complete **Creative Commons CC0 1.0 Universal**
text, beginning `# Creative Commons CC0 1.0 Universal`. That is the asset
grant this project relies on.

### One source did not resolve

`http://static.makehumancommunity.org/mpfb/faq/can_i_use_mpfb_for_a_closed_source_game.html`
returned **HTTP 404**. It is recorded here as attempted-and-missing rather
than quietly dropped. The CC0 grant does not depend on it: `LICENSE.ASSETS.md`
in the official repository is the primary evidence, and the MakeHuman
Community licence page is retained as corroboration.

## Installed package cross-check

From `scripts/blender/mpfb_probe.py`, run against the actual installation:

```
package    %APPDATA%\Blender Foundation\Blender\5.2\extensions\blender_org\mpfb
id         mpfb          version 2.0.17        build 20260722
manifest   license = ["SPDX:GPL-3.0-or-later"]
assets     targets 1445 · rigs 18 · uv_layers 13 · textures 12
           mesh_metadata 6 · poses 5 · node_trees 3 · 3dobjs 1 · expressions 1
```

The four `license.json` files inside the package are MakeClothes / MakeSkin /
MakeExpression **asset-authoring templates** — metadata written into assets a
user creates. They are not the package's asset grant, which is why the
repository's `LICENSE.ASSETS.md` was needed.

## What is deliberately not committed

The MPFB extension directory, its cache, the downloaded archive, the system
asset directories and Blender user preferences are all excluded. Only the
licence texts above are versioned, under `third-party-notices/mpfb/`.

---

## Correction, 2026-08-07: system assets are NOT bundled

An earlier version of this document stated that the MPFB extension bundles
the official MakeHuman core assets and that no separate asset pack was needed.
**That was wrong**, and it was wrong because it inferred from directory names
rather than asking MPFB.

MPFB's own check, run against the installation:

```
AssetService.check_if_modern_makehuman_system_assets_installed()  ->  (False, False)
AssetService.get_asset_roots()                                    ->  []
get_asset_list: skins 0 · eyes 0 · teeth 0 · proxymeshes 0 · clothes 0
```

What the 43 MB extension actually ships is **system data**, not the asset
pack: `3dobjs` (base mesh), `targets` (1445 morphs), `rigs` (18 definitions),
`node_trees`, `textures`, `uv_layers`, `mesh_metadata`, `poses`, `expressions`,
`settings`.

That is enough to build and rig a body. It is **not** enough to skin one.

### Consequence for the material question

`materialCount = 0` on the MPFB output is **not** a GLB round-trip loss — an
earlier report claimed that and the claim was unfounded. Measured at stage A,
immediately after `create_human` + `add_builtin_rig` and before any export:

```
STAGE_A_MATERIALS  0  []
```

The mesh has no material because no skin was ever applied, and no skin was
applied because none is installed. `HumanService.set_character_skin` takes an
`.mhmat` file path, and there are no `.mhmat` files on this machine.

### What this needs

The official **MakeHuman system assets** pack, from the MakeHuman Community
asset page, under the same rules as the extension: official source only,
recorded URL / bytes / SHA-256, no third-party contributed packs.

`MaterialService.create_v2_skin_material` can build a procedural skin without
the pack, but that is not the official core skin that was asked for, so it is
noted as an option rather than taken.
