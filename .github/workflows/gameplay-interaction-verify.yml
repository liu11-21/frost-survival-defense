name: Gameplay Interaction Verification

on:
  pull_request:
    branches:
      - main
    paths:
      - "src/**"
      - "tests/scene-interaction-rework.spec.ts"
      - "tests/lane-deployment-recovery.spec.ts"
      - "package.json"
      - "package-lock.json"
      - "playwright.config.ts"
      - ".github/workflows/gameplay-interaction-verify.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gameplay-interaction:
    name: Real pointer, recruit drag and deployment-policy regression
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use repository Node version
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: TypeScript check
        run: npm run typecheck
      - name: Production build
        run: npm run build
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Start production preview
        shell: bash
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 > "$RUNNER_TEMP/gameplay-interaction-preview.log" 2>&1 &
          echo $! > "$RUNNER_TEMP/gameplay-interaction-preview.pid"
          for attempt in $(seq 1 60); do
            if curl --fail --silent http://127.0.0.1:4173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat "$RUNNER_TEMP/gameplay-interaction-preview.log"
          exit 1
      - name: Pointer, recruit drag and deployment-policy regression
        run: |
          mkdir -p .runtime/g1-evidence .runtime/deployment-policy
          npx playwright test tests/scene-interaction-rework.spec.ts tests/lane-deployment-recovery.spec.ts
      - name: Upload runtime evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: gameplay-interaction-verification-${{ github.run_id }}
          path: |
            .runtime/g1-evidence/
            .runtime/deployment-policy/
            test-results/
            ${{ runner.temp }}/gameplay-interaction-preview.log
          if-no-files-found: warn
