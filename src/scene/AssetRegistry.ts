/** Backwards-compatible import path for older gameplay modules. */
export { AssetRegistry } from "../assets/AssetRegistry";
export type { AssetInstance, AssetKey, AssetSpec, AssetValidationReport } from "../assets/AssetTypes";
