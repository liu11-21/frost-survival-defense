/** Shared DOM drag-and-drop MIME key. Keeping the string in one tiny module
 * prevents the recruit menu and canvas input router from silently drifting. */
export const RECRUIT_DRAG_MIME = "application/x-frostbound-recruit";
